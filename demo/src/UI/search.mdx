---
name: Search
route: /ui/search
menu: UI
tags: ui, search, user interface
---

import { Hint, InlineCode } from '@nejcm/docz-theme-extended';
import searchPic from '../../public/assets/search.jpg';

# Search

Search allows users to search documents by name or tags. The search button is displayed inside the header bar.

<img src={searchPic} alt="Search" />

## Config

### Search

Inside `doczrc.js` file there is a `themeConfig.search` option to enable or disable search.

```js
//doczrc.js
export default {
  themeConfig: {
    search: true,
  },
};
```

#### Options

| Name   |   Type    | Default | Description                            |
| :----- | :-------: | :-----: | :------------------------------------- |
| search | `boolean` | `true`  | Specify if the search should be shown. |

### Tags

Tags are additional information inside each document that are searchable. They can be separated with commas.

```mdx
---
name: Footer
route: /ui/footer
menu: UI
tags: ui, footer, footer config, theme, user interface
---

# My mdx file
```

#### Options

| Name |   Type   | Default | Description                         |
| :--- | :------: | :-----: | :---------------------------------- |
| tags | `string` |         | Tags separated by commas or spaces. |
