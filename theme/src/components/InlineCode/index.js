import React from 'react';
import {Container} from './styles';

const InlineCode = (props) => <Container {...props} />;

InlineCode.displayName = 'InlineCode';
InlineCode.propTypes = {};

export {InlineCode};
